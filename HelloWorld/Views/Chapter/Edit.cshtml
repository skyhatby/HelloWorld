@model HelloWorld.Models.Chapter

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Chapter</legend>

        @Html.HiddenFor(model => model.ChapterId)

        <div class="editor-label">
            @Html.LabelFor(model => model.ChapterName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChapterName)
            @Html.ValidationMessageFor(model => model.ChapterName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ChapterContent)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChapterContent)
            @Html.ValidationMessageFor(model => model.ChapterContent)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.BookId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BookId)
            @Html.ValidationMessageFor(model => model.BookId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
